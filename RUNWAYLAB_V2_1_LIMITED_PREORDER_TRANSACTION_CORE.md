# RunwayLab V2.1 Limited Preorder Transaction Core

基线：V2.0B.6，commit `cb13c716fb4cab97901fe56a7b5cfb3ef1756f84`

## 产品定义

V2.1 的销售模式是限量预售与达标生产，不是投资、股权或收益型众筹。

用户支付用于购买具体商品。活动在截止时间达到最低数量后才进入生产；未达标订单进入取消与退款流程。

## 第一批交易核心

本批只建立支付渠道无关的业务规则：

- 活动状态：草稿、排期、进行中、达标、未达标、生产、关闭、取消。
- 订单状态：创建、等待支付、预订、确认生产、生产中、完成、取消。
- 支付状态：未支付、处理中、已授权、已扣款、退款中、已退款、失败。
- 履约状态：未开始、排产、生产、质检、发货、送达、取消。
- 金额统一使用最小货币单位整数，禁止浮点金额。
- 创建订单、创建支付、支付回调和退款必须提供幂等键。
- 非法状态跳转必须拒绝，不允许后台任意改状态。

## 风险边界

- 未选择真实支付渠道前，不启用 live payment。
- 支付回调不能直接相信请求体，必须验签、去重并在事务内更新。
- 活动达标以数据库事务内的有效订单数量为准，不能相信页面计数。
- 达标后价格、SKU、最低数量与截止时间必须锁定。
- 未达标退款、用户主动取消和平台风控取消是三种不同原因，后续必须保留审计记录。
- 已发货订单不能直接走自动退款。
- 平台结算不能与支付成功同时发生，需等待交付或售后窗口结束。

## 后续批次

1. Schema 与 migration：支付尝试、退款、幂等记录、状态事件。
2. 服务层：事务化创建订单、支付回调、达标结算、失败退款。
3. 管理端：活动锁定、异常订单、人工复核。
4. 隔离 PostgreSQL E2E。
5. 选择支付渠道后实现适配器，最后才允许生产开关。
